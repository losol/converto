{
  "compilerOptions": {
    "target": "ESNext",
    "strict": true
  },

  "include": ["custom.d.ts"],

  "exclude": [
    "node_modules/",
    "dist/",

    // Do not include server files in the server compilation
    "server/",
    // Do not include test files
    "**/*.test.ts"
  ]
}
